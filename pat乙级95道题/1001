#include <iostream>
using namespace std;

int main(){
  int n;
  int m = 0;
  cin>>n;
  while(n != 1){
    //int m = 0;
    if(n%2 == 0){
      n = n/2;
     // m += 1;
    }
    else{
      n = (3*n + 1)/2;
     // m += 1;
    }
    m++;
  }
  cout<< m<<endl;
}
