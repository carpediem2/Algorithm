#include<iostream>
using namespace std;

int main(){
	int a[110],n,m;
	cin>>n>>m;
	m = m % n;
	for(int i = m;i<n;i++){
		cin>>a[i];
	}
	for(int i=0;i<m;i++){
		cin>>a[i];
	}
	for(int i=0;i<n;i++){
		cout<<a[i];
		if(i < n-1){
			cout<<" ";
		}
	}
	return 0;
}


第七行： m %= n;前面不能重复加Int
头文件要加<iostream>
C++中的读入引入为cin>>a[i]
